export function productOptions(state) {
  return state.product.Options
}

export function isShowingBottomTab(state) {
  return state.isShowingBottomTab
}

// 사용자가 고른 옵션들
export function selectedOptions(state) {
  return state.selectedOptions
}